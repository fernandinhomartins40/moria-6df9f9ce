# Arquivo de variáveis de ambiente para deploy em VPS
# Copie este arquivo para .env antes de rodar docker-compose.vps.yml
# IMPORTANTE: Altere TODOS os valores de produção antes de usar!

# ============================================
# POSTGRES (Banco de Dados)
# ============================================
POSTGRES_USER=moria
POSTGRES_PASSWORD=ALTERAR_SENHA_FORTE_AQUI_MIN_32_CARACTERES
POSTGRES_DB=moria_db

# ============================================
# BACKEND (API)
# ============================================

# Database URL completa - substituir com os valores do POSTGRES acima
DATABASE_URL=postgresql://moria:ALTERAR_SENHA_FORTE_AQUI_MIN_32_CARACTERES@postgres:5432/moria_db?schema=public

# JWT Secret - IMPORTANTE: usar senha forte de pelo menos 64 caracteres
JWT_SECRET=ALTERAR_JWT_SECRET_SUPER_FORTE_MIN_64_CARACTERES_USAR_GERADOR_SENHA

# Tempo de expiração dos tokens
JWT_EXPIRES_IN=7d
JWT_ADMIN_EXPIRES_IN=8h

# CORS - URLs do frontend que podem acessar a API
# Formato: https://seudominio.com,https://www.seudominio.com
CORS_ORIGIN=https://moriapescaservicos.com,https://www.moriapescaservicos.com
ALLOWED_ORIGINS=https://moriapescaservicos.com,https://www.moriapescaservicos.com

# URL do frontend (para redirecionamentos)
FRONTEND_URL=https://moriapescaservicos.com

# ============================================
# DEPLOY OPTIONS
# ============================================

# Executar seed do banco? (true/false)
# IMPORTANTE: Usar 'true' apenas na primeira vez ou para popular dados de exemplo
# Em produção com dados reais, deixar como 'false'
RUN_SEED=false

# Build timestamp (gerado automaticamente)
BUILD_TIMESTAMP=
