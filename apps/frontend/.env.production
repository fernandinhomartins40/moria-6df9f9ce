# Production environment
# Em produção, o frontend usa caminho relativo /api
# O Nginx recebe /api/* e faz rewrite removendo /api antes de enviar para o backend
VITE_API_BASE_URL=/api
VITE_APP_ENV=production
VITE_APP_VERSION=1.0.0
