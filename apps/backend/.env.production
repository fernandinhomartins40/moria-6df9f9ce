# Production Environment Configuration
# IMPORTANTE: Este arquivo contém valores padrão para produção
# Substitua os valores sensíveis (senhas, secrets) em produção

# Application
NODE_ENV=production
PORT=3001

# Database
# ATENÇÃO: Substitua com as credenciais reais de produção
DATABASE_URL=postgresql://moria:moria_prod_2024_CHANGE_ME@postgres:5432/moria_db?schema=public

# JWT
# ATENÇÃO: OBRIGATÓRIO mudar este secret para um valor seguro de 32+ caracteres
JWT_SECRET=moria_jwt_secret_PRODUCTION_CHANGE_THIS_TO_SECURE_VALUE_32_CHARS_MINIMUM
JWT_EXPIRES_IN=7d

# Bcrypt
BCRYPT_ROUNDS=12

# CORS
# ATENÇÃO: Substitua com o domínio real de produção
CORS_ORIGIN=http://localhost:3090,https://seu-dominio.com,https://www.seu-dominio.com

# Logging
LOG_LEVEL=info
